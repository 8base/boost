import React from 'react';

import { NoData } from '../../';

export default {
  title: 'Components/NoData',
  component: NoData,
};

export const defaultStory = () => <NoData />;

defaultStory.story = {
  name: 'default',
};
