import React from 'react';

import { NoData } from '../../';

export default {
  title: 'Components/NoData',
};

export const defaultStory = () => <NoData />;

defaultStory.story = {
  name: 'default',
};
